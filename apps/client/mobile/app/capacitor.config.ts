import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.virtex.mobile',
  appName: 'virtexMobile',
  webDir: '../../dist/apps/virtex-mobile'
};

export default config;
