import { fixedAssetsDomain } from '@virteex/fixed-assets-domain/lib/fixed-assets-domain';

describe('fixedAssetsDomain', () => {
  it('should work', () => {
    expect(fixedAssetsDomain()).toEqual('fixed-assets-domain');
  })
})
