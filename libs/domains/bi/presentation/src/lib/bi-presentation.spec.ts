import { biPresentation } from '@virteex/bi-presentation/lib/bi-presentation';

describe('biPresentation', () => {
  it('should work', () => {
    expect(biPresentation()).toEqual('bi-presentation');
  })
})
