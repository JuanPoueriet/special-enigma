import { crmPresentation } from '@virteex/crm-presentation/lib/crm-presentation';

describe('crmPresentation', () => {
  it('should work', () => {
    expect(crmPresentation()).toEqual('crm-presentation');
  })
})
