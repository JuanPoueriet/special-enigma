import { projectsDomain } from '@virteex/projects-domain/lib/projects-domain';

describe('projectsDomain', () => {
  it('should work', () => {
    expect(projectsDomain()).toEqual('projects-domain');
  })
})
