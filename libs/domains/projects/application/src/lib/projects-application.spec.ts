import { projectsApplication } from '@virteex/projects-application/lib/projects-application';

describe('projectsApplication', () => {
  it('should work', () => {
    expect(projectsApplication()).toEqual('projects-application');
  })
})
