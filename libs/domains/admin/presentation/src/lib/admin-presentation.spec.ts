import { adminPresentation } from '@virteex/admin-presentation/lib/admin-presentation';

describe('adminPresentation', () => {
  it('should work', () => {
    expect(adminPresentation()).toEqual('admin-presentation');
  })
})
