import { join } from 'path';

export const IDENTITY_PROTO_PATH = join(__dirname, 'lib/identity.proto');
export const IDENTITY_PACKAGE = 'identity';

export const BILLING_PROTO_PATH = join(__dirname, 'lib/billing.proto');
export const BILLING_PACKAGE = 'billing';

export const INVENTORY_PROTO_PATH = join(__dirname, 'lib/inventory.proto');
export const INVENTORY_PACKAGE = 'inventory';
